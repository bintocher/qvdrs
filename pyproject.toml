[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "qvd"
version = "0.1.0"
description = "Fast QVD file reader/writer with EXISTS() support, powered by Rust"
requires-python = ">=3.8"

[tool.maturin]
features = ["python"]
